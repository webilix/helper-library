import { AREA } from './area';
import { LENGTH } from './length';
import { VOLUME } from './volume';
import { WEIGHT } from './weight';

export const UNIT = {
    AREA,
    LENGTH,
    VOLUME,
    WEIGHT,
};
